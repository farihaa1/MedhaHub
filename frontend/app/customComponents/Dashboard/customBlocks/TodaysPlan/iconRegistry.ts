import { BookOpen, Calculator, Languages, LucideIcon } from "lucide-react"

export const iconRegistry: Record<string, LucideIcon> = {
  book: BookOpen,
  language: Languages,
  calculator: Calculator,
}
